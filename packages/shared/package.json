{
  "name": "@pathket/shared",
  "version": "1.0.0",
  "description": "Shared code for Pathket (types, services, hooks, utils)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0",
    "date-fns": "^2.30.0",
    "zod": "^3.22.0",
    "zustand": "^4.4.0",
    "immer": "^10.0.0",
    "uuid": "^9.0.0"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.7",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
